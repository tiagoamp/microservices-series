spring:
  application:
    name: USER-INFORMATIVON-SERVICE
  datasource:
    url: jdbc:h2:mem:userdb
    driverClassName: org.h2.Driver
    username: admin
    password: admin
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true

server:
  port: 8082
